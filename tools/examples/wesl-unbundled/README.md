# WESL in Vanilla Javascript

This example shows how you can use [WESL](https://wesl-lang.dev/) at runtime. It combines multiple `.wesl` shaders together into one WGSL shader.

If you view the `index.html` with a HTTP server in a WebGPU enabled browser, then you should see a picture of a mandelbrot set.
