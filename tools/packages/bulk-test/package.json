{
  "name": "bulk-test",
  "private": true,
  "type": "module",
  "scripts": {
    "bulk-test": "vitest run",
    "test": "vitest",
    "test:once": "vitest run",
    "typecheck": "tsgo"
  },
  "dependencies": {
    "wesl": "workspace:*"
  }
}
