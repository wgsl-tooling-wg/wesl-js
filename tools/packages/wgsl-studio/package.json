{
  "name": "wgsl-studio",
  "description": "WGSL/WESL previews and tests",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "repository": "github:wgsl-tooling-wg/wesl-js",
  "scripts": {
    "build": "tsdown && cp ../wgsl-test/dist/weslBundle.js dist/",
    "build:play": "pnpm --filter wgsl-play build",
    "build:vsix": "run-s build:play build build:vsix:package",
    "build:vsix:package": "vsce package",
    "dev": "tsdown --watch",
    "launch": "run-s build:play build launch:code",
    "launch:code": "code --extensionDevelopmentPath=$PWD",
    "test:vscode": "pnpm build && vscode-test",
    "typecheck": "tsgo"
  },
  "main": "./dist/extension.mjs",
  "dependencies": {
    "webgpu": "^0.3.8"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/vscode": "^1.108.1",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "@vscode/vsce": "^3.7.1",
    "wesl": "workspace:x",
    "wesl-gpu": "workspace:x",
    "wesl-tooling": "workspace:x",
    "wgsl-play": "workspace:x",
    "wgsl-test": "workspace:x"
  },
  "license": "MIT",
  "keywords": [
    "test",
    "WebGPU",
    "WESL",
    "WGSL"
  ],
  "activationEvents": [
    "onLanguage:wgsl"
  ],
  "categories": [
    "Programming Languages",
    "Debuggers",
    "Visualization"
  ],
  "contributes": {
    "commands": [
      {
        "command": "wgsl.previewToyShader",
        "title": "WGSL Studio: Preview Toy Shader"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "wgsl.previewToyShader",
          "when": "editorLangId == wgsl",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "wgsl.previewToyShader",
          "when": "resourceExtname == .wgsl || resourceExtname == .wesl",
          "group": "navigation"
        }
      ]
    },
    "languages": [
      {
        "id": "wgsl",
        "extensions": [
          ".wgsl",
          ".wesl"
        ]
      }
    ]
  },
  "displayName": "WGSL Studio",
  "engines": {
    "vscode": "^1.108.0"
  },
  "icon": "images/logo-small.png",
  "publisher": "webgpu-tools"
}
